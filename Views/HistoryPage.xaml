<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:constants="clr-namespace:XamarinFormsSample;assembly=XamarinFormsXamlSample" x:Class="Wineot.HistoryPage" Title="Historique">
	<ContentPage.Content>
		<ListView x:Name="WineListView" SeparatorVisibility="None" RowHeight="84"  BackgroundColor="#eee" >
			<ListView.ItemTemplate>
				<DataTemplate>
					<ViewCell>
						<StackLayout Orientation="Vertical">
							<StackLayout Orientation="Horizontal" BackgroundColor="White" Spacing="2">
								<Image Source="background.jpg" HeightRequest="84"/>
								<StackLayout Orientation="Vertical" Spacing="2">
									<Label Text="{Binding winery.name}" FontSize="12"/>
									<Label Text="{Binding name}" />
									<StackLayout Orientation="Horizontal">
										<Label Text="{Binding winery.country}" FontSize="12"/>
										<Label Text="-" FontSize="12"/>
										<Label Text="{Binding winery.region}" FontSize="12"/>
									</StackLayout>
								</StackLayout>
							</StackLayout>
						</StackLayout>
					</ViewCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</ContentPage.Content>
</ContentPage>